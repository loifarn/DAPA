package dapa.messagetypes;

public enum MessageType {
    JOIN,
    PART,
    CLOSE,
    ERROR,
    LOGIN,
    REG,
    PRIVMSG,
    CHANMSG,
    CONNECTED,
    WELCOME,
    CREATE, WHO, HEARTBEAT,
}
